services:
  - type: web
    name: obra-report
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 60
    envVars:
      - key: BREVO_API_KEY
        sync: false
      - key: FROM_EMAIL
        value: obrareport@gmail.com
      - key: FROM_NAME
        value: Informes de Obra
